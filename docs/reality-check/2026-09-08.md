# Next steps reality check: 2026-09-08

## Verdict

Build on the accepted Comb implementation. The next Comb task is landing and packaging the tested branch, not another storage redesign. Foundation has progressed beyond the earlier annotation-identity blockers to an accepted local packaged-desktop gate. Its current test run is not green, however, and includes one concrete SQLite contention failure in addition to timeouts.

## Scorecard

| Claim | Status | Evidence |
|---|---|---|
| Comb has an accepted process boundary and real backend/replay proof | Verified | `62689ce`, CI34041204689 success checked today; [six-run receipt](../implementation/reliable-log/verification/foundation-process-gate.json); fresh Node caller/key run 24 passed |
| Foundation has recorded real host and packaged desktop acceptance | Verified | Foundation `0873896`; `docs/review/foundation-desktop-integration-gate.md` and committed `foundation-desktop/report.json`: seven checks, real handler/preload/HTTP/Comb route, clean shutdown |
| Foundation focused host checks currently pass | Broken | Fresh command below: 42 passed, 16 failed; typecheck separately passes |
| Accepted Comb work is landed on main | Missing | Live GitHub main is d9aa5eb; 62689ce is not its ancestor. Local main is b77b64e, accepted branch was 87 commits ahead of that checkout; live `gh pr list` returns no open PR |
| Comb has a versioned binary-release workflow for consumers | Missing | `.github` contains only `workflows/ci.yml`, with tests/lint and no release artifact job; Foundation acceptance uses an explicitly hashed copied executable |

Counts: 2 verified, 0 partial, 2 missing, 1 broken, 0 unverifiable within this five-claim scope. The failed claim concerns today's checks, not a conclusion that all host behavior is broken.

## Fresh checks

- Comb: `node --test scripts/foundation-bridge-key.test.mjs scripts/foundation-bridge-client.test.mjs` passes all 24 tests.
- Foundation at `0873896`: `pnpm typecheck` exits zero.
- Foundation: `pnpm exec vitest run packages/host/test packages/cli/test/host.test.ts` exits one after 151.29 seconds. Six files ran, four failed. Totals: 42 passed, 16 failed.
- Most reported failures are Vitest's five-second timeout. The concurrent Node writers test additionally reports a source assertion failure: child exits 1 because `ReplicaStore.transaction` at `packages/host/src/replica-store.ts:559` cannot execute BEGIN IMMEDIATE. Error is SQLite code 5, database is locked. The observed child runtime is Node 25.8.0; the accepted packaged runtime is Node 24.20.0. Runtime and machine-load effects have not been isolated, so neither a product regression nor a harmless flake is established.
- Foundation's own acceptance report already disclaims a final full-suite green run. It records earlier process/IO timeouts. The current concurrency assertion should not be reduced to that historical timeout explanation.

No production files changed and no shared process or backend lifecycle was touched. The fresh Foundation test process completed normally with its failure exit.

## Dispositions

- Comb Core/Log/bridge: build on. Existing immutable acceptance and current CI remain valid.
- Foundation host/desktop: build on; diagnose the test failures before landing. No basis here to discard its journal or replica-identity design.
- Delivery: finish the missing branch landing and versioned binary workflow. Live GitHub mains are Comb d9aa5eb and Foundation 9b92760. Ancestry checks confirm that neither contains its accepted integration checkpoint, respectively 62689ce and 0873896. Neither live PR listing showed an open PR.

## Next moves

1. Open and land the accepted Comb branch with its verification report. Update the README's v2-only status to distinguish v2 legacy paths and the supported v3 complete-feed bridge. Do not merge the rejected old retry or bridge branches.
2. In parallel, have the Foundation owner reproduce SQLite contention with the pinned runtime and a controlled test run. Resolve genuine failures and explicit timeout budgets, then land its host/desktop branch. Existing desktop proof does not replace a repeatable test gate.
3. Add a versioned Comb bridge build, checksums and a short installation/compatibility contract so Foundation can pin a release. Then use Pheromone ObjectLog as the next consumer, with a separate bounded-replay and I/O-ownership gate.

Keep Tree, Volume, hosting expansion, physical stable batching and destructive GC deferred. No new storage abstraction is required to finish the current delivery path.

## Limits

This was a bounded status check. I inspected live branches, PR/CI state, package/workflow definitions and committed acceptance artifacts, and ran the checks above. I did not rerun the six remote backend captures, reopen the native desktop UI, inspect every Foundation code path or diagnose the SQLite failure. Human native mouse/keyboard testing remains useful for the local prototype; its previous automated desktop gate used DOM events through real renderer handlers.
